{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    // Deliberately empty: `src` must not reach for node or DOM globals. Scripts
    // and tests get those via tsconfig.tools.json.
    "types": []
  },
  "include": ["src/**/*.ts"]
}
